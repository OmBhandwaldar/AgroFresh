export default async function CategoriesPage() {
  return (
    <main>
      {/* <h1>Shop by Category</h1>
      <ul>
        {categories.map(cat => (
          <li key={cat.slug}>
            <Link href={`/categories/${cat.slug}`}>
              {cat.name}
            </Link>
          </li>
        ))}
      </ul> */}
      <div>sdaf</div>
    </main>
  );
}
